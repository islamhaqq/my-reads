import React from 'react';

/**
 * Simply displays the title of the app. First thing users look at when they
 * launch the app.
 * @method Header
 */
const Header = () => (
  <div>
    <h1>My Reads</h1>
  </div>
);

export default Header;
